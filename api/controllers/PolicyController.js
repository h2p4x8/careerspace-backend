/**
 * PolicyController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `PolicyController.isAdmin()`
   */
  isAdmin: async function (req, res) {
    return res.json({
      todo: 'isAdmin() is not implemented yet!'
    });
  }

};

